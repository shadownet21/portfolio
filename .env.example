# No environment variable is required for version 1.
# Add server-only secrets here in a future version; never prefix secrets with NEXT_PUBLIC_.

# Public canonical URL used for metadata and the sitemap.
# NEXT_PUBLIC_SITE_URL=https://votre-domaine.ca
